<div class="icon-container">
</div>
<div class="event-body">
  <div class="event-heading">
    <h5>{{eventMessage}}</h5>
    <time>{{moment-from-now event.createdAt}}</time>
  </div>
  <p>{{event.book.title}}</p>
  {{list-tags tags=event.book.tags library=userLibrary}}
</div>
